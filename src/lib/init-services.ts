// Import this file once at the root of the app to ensure all EventBus listeners are attached
import './services/scoring'
import './services/gamification'
import './services/notifications'

console.log("Services initialized and EventBus listeners attached.")
